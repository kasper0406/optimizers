# Virtual environments
.venv/
venv/

# Python bytecode / caches
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# macOS
.DS_Store

# NOTE: results/ and uv.lock are intentionally NOT ignored.
# results/ is append-only synced data; uv.lock is the dependency pin.
sweeps/  # generated sweep expansions (regenerable from configs)
